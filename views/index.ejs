<!DOCTYPE html>
<html>
<head>
    <title>Dsheet Home</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1 class="mb-4">ようこそDsheetへ</h1>

        <% if (error) { %>
            <div class="alert alert-danger"><%= error %></div>
        <% } %>

        <% if (!user) { %>
            <div class="card p-4 mb-4">
                <form action="/register" method="post">
                    <h2 class="mb-3">ユーザー登録</h2>
                    <div class="mb-3">
                        <label for="registerUsername" class="form-label">ユーザー名</label>
                        <input type="text" class="form-control" id="registerUsername" name="username" placeholder="ユーザー名" required>
                    </div>
                    <div class="mb-3">
                        <label for="registerPassword" class="form-label">パスワード</label>
                        <input type="password" class="form-control" id="registerPassword" name="password" placeholder="パスワード" required>
                    </div>
                    <button type="submit" class="btn btn-primary">登録</button>
                </form>
            </div>

            <div class="card p-4">
                <form action="/login" method="post">
                    <h2 class="mb-3">ログイン</h2>
                    <div class="mb-3">
                        <label for="loginUsername" class="form-label">ユーザー名</label>
                        <input type="text" class="form-control" id="loginUsername" name="username" placeholder="ユーザー名" required>
                    </div>
                    <div class="mb-3">
                        <label for="loginPassword" class="form-label">パスワード</label>
                        <input type="password" class="form-control" id="loginPassword" name="password" placeholder="パスワード" required>
                    </div>
                    <button type="submit" class="btn btn-success">ログイン</button>
                </form>
            </div>
        <% } else { %>
            <h2 class="mt-4">こんにちは、<%= user.username %>さん</h2>
            <form action="/logout" method="post" class="mt-3">
                <button type="submit" class="btn btn-danger">ログアウト</button>
            </form>
            <a href="/checklist" class="btn btn-primary mt-3">チェックリストを見る</a>
        <% } %>
    </div>
</body>
</html>
